import MainContainer from "../../components/containers/MainContainer";

const Dashboard = () => {
  return (
    <MainContainer>
      <span>Dashboard</span>
    </MainContainer>
  );
};

export default Dashboard;
